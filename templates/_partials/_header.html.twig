<header class="bg-white shadow-md">
    <div class="container mx-auto px-6 py-4 flex justify-between items-center">
        <!-- Logo -->
        <a href="{{ path('home') }}" class="text-2xl font-bold text-blue-500">
            📖 ReBooks
        </a>

        <!-- Navigation -->
        <nav class="hidden md:flex space-x-6">
            <a href="#" class="text-gray-900 hover:text-blue-500">Accueil</a>
            <a href="#" class="text-gray-900 hover:text-blue-500">Catégories</a>
            <a href="#" class="text-gray-900 hover:text-blue-500">À propos</a>
        </nav>

        <!-- Actions -->
        <div class="flex items-center space-x-4">
            <!-- Champ de recherche -->
            <div class="relative">
                <label>
                    <input type="text" placeholder="Rechercher..."
                           class="border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring focus:ring-blue-200">
                </label>
                <button class="absolute right-2 top-2 text-gray-500">
                    🔍
                </button>
            </div>

            <!-- Bouton Connexion / Profil -->
            <a href="{{ path('app_login') }}" class="bg-green-500 text-white px-4 py-2 rounded-lg hover:bg-green-600">
                Connexion
            </a>
        </div>
    </div>
</header>
